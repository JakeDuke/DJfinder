

let view = { 
	render: function(artist) {
		console.log(artist);
	},
	render_component: function(container_id, data_to_render_with) {
		// get container
		// build new dom elements with data
		// append dom elements
		// into a boostrap grid
	}
}

module.exports = view;